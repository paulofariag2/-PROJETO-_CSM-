# Projeto CSM - versão final

## Tecnologias
- Backend: Node.js, Express, MySQL (mysql2), JWT, bcrypt
- Frontend: HTML, CSS, JS (fetch)

## Como rodar (local)
1. Criar banco MySQL com `sql/database.sql`.
2. Copiar `backend/.env.example` para `backend/.env` e ajustar credenciais.
3. No terminal:
